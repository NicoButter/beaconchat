<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme using AppCompat for Material3 compatibility -->
    <style name="Theme.BeaconChat" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>