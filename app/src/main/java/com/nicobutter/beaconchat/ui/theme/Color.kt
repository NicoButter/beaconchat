package com.nicobutter.beaconchat.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Material Design 3 color palette for BeaconChat theme.
 *
 * Contains light theme color definitions following Material Design guidelines.
 * These colors provide a consistent visual identity across the application.
 */

// Light theme colors (80% opacity variants for light backgrounds)
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

// Dark theme colors (40% opacity variants for dark backgrounds)
val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

/**
 * BeaconChat custom color palette.
 *
 * Custom colors specifically designed for the BeaconChat application,
 * providing a unique visual identity while maintaining accessibility.
 */
val BeaconPrimary = Color(0xFF6200EE)        // Deep purple primary color
val BeaconSecondary = Color(0xFF03DAC6)      // Teal secondary color
val BeaconBackground = Color(0xFFFFFFFF)     // White background
val BeaconSurface = Color(0xFFFFFFFF)        // White surface color
val BeaconOnPrimary = Color(0xFFFFFFFF)      // White text on primary
val BeaconOnBackground = Color(0xFF000000)   // Black text on background
val BeaconOnSurface = Color(0xFF000000)      // Black text on surface